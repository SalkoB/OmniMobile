## Packages
framer-motion | Page transitions and scroll-triggered animations
react-helmet | Managing document head for SEO (Title, Schema.org)

## Notes
Static assets are imported from @assets/
Schema assumes LocalBusiness JSON-LD is injected into the DOM
